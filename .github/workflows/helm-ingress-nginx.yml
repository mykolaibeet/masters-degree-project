name: "Helm ingress-nginx"
 
on:
 push:
   paths:
   - infra/helm/ingress-nginx/**
   - .github/workflows/helm-ingress-nginx.yml

jobs:
 helm:
   name: "Helm ingress-nginx"
   runs-on: ubuntu-latest
   container:
     image: docker://alpine/helm:3.11.1
     volumes:
       - infra/helm/ingress-nginx:/opt/app

   steps:
     - name: Deploy
       run: upgrade -i --dry-run ingress-nginx . -f values.yml -n ingress-ngins
       working-directory: /opt/app
       env:
         KUBECONFIG_FILE: "${{ secrets.KUBECONFIG }}"

     - name: Deploy
       if: github.ref == 'refs/heads/master'
       run: upgrade -i --dry-run ingress-nginx . -f values.yml -n ingress-ngins
       working-directory: /opt/app
       env:
         KUBECONFIG_FILE: "${{ secrets.KUBECONFIG }}"
