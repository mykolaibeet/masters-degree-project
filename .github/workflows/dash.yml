name: "Dash Test, Build, Deploy"
 
on:
 push:
   paths:
   - apps/dash/**
   - .github/workflows/dash.yml

jobs:
  test:
   name: "Dash test"
   runs-on: ubuntu-latest

   steps:
     - name: Checkout repo
       uses: actions/checkout@v4

     - name: Test
       run: echo "Tests passed successfully"

  build:
   name: "Dash build"
   runs-on: ubuntu-latest
   needs: [test]
   container:
     image: docker://docker:dind
     volumes:
       - ${{ github.workspace }}:/app

   steps:
     - name: Checkout repo
       uses: actions/checkout@v4

     - name: Build
       run: cd apps/dash/ && docker build -t registry.digitalocean.com/masters-degree/dash:${{github.sha}} .

     - name: Login
       run: docker login registry.digitalocean.com --username=${{ secrets.DIGITALOCEAN_ACCESS_TOKEN }} --password=${{ secrets.PASSWORD }}

     - name: Push
       run: docker push registry.digitalocean.com/masters-degree/dash:${{github.sha}}
  
  deploy:
   name: "Dash deploy"
   runs-on: ubuntu-latest
   needs: [build]
   container:
     image: docker://alpine/helm:3.11.1
     volumes:
       - ${{ github.workspace }}:/app

   steps:
     - name: Checkout repo
       uses: actions/checkout@v4

     - name: Deploy
       run: cd apps/dash/ && mkdir -p /github/home/.kube && echo $KUBECONFIG_FILE | base64 -d  > /github/home/.kube/config && helm upgrade -i dash helm -f helm/values.yaml -n default --set image.tag=$${{github.sha}}
       env:
         KUBECONFIG_FILE: ${{ secrets.KUBECONFIG }}